#include "BC.hpp"

class Activation {
private:
    int pc;
    vector<string> local_variable_values;
public:
    Activation() = default;
    ~Activation() = default;
};

class Interpreter {
private:
public:
    Interpreter() = default;
    ~Interpreter() = default;

};